<!DOCTYPE html>
<html lang="<%= stati.site.defaultLocale || 'en' %>">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= stati.page.title ? `${stati.page.title} | ${stati.site.title}` : stati.site.title %></title>

    <!-- Essential meta tags -->
    <% if (stati.page.description) { %>
    <meta name="description" content="<%= stati.page.description %>" />
    <% } %>

    <!-- Canonical URL -->
    <link rel="canonical" href="<%= stati.site.baseUrl %><%= stati.page.url %>" />

    <!-- Styles -->
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <a href="#main" class="skip-link">Skip to main content</a>

    <header role="banner">
      <nav role="navigation">
        <a href="/" class="logo"><%= stati.site.title %></a>
      </nav>
    </header>

    <main id="main" role="main"><%~ stati.content %></main>

    <footer role="contentinfo">
      <p>&copy; <%= new Date().getFullYear() %> <%= stati.site.title %>. Built with <a href="https://github.com/ianchak/stati">Stati</a> v<%= stati.generator.version %>.</p>
    </footer>
  </body>
</html>
